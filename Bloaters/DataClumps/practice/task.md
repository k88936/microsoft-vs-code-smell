# Introduce
In this small task, you need to encapsulate data clumps together in a class.

This is a small demo that compute some norm of a 3d vector.

# Task

Imagine that if we wanted to restrict param y_coordinate to positive numbers only, or add extra dimension we may need to
edit the code in many places.

How we should refactor is simple:

* create a class Coordinates to hold all these single float variables